apiVersion: v1
kind: Service
metadata:
  name: catchpoint-exporter
  labels:
    app: catchpoint-exporter
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: catchpoint-exporter
